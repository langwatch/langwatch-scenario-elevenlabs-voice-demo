{
  "name": "scenario-050-repro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone clean-room repro proving @langwatch/scenario@0.5.0 works for a hosted-ElevenLabs multi-turn voice scenario.",
  "scripts": {
    "run": "node --env-file=.env --import tsx src/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.110",
    "@langwatch/scenario": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
